type photoCreate = {
    title: string,
    image_url: string,
    description: string,
    created_at: string
}

type photo = {
    id: number,
    title: string,
    description: string,
    photoUrl: string,
    date: string
}