type photoCreate = {
    title: string,
    description: string,
    photoUrl: string,
    date: string
}

type photo = {
    id: number,
    title: string,
    description: string,
    photoUrl: string,
    date: string
}