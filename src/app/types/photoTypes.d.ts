type photoCreate = {
    title: string,
    image_url: string,
    description: string,
}

type photo = {
    id: number,
    title: string,
    description: string,
    image_url: string,
    date: string
}